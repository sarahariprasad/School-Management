SECRET_KEY=replace-this-with-a-long-random-secret
DEBUG=True
DJANGO_ENV=development
ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=

# Required when DJANGO_ENV=production (PostgreSQL)
DB_NAME=school_erp
DB_USER=school_erp
DB_PASSWORD=replace-with-a-strong-password
DB_HOST=127.0.0.1
DB_PORT=5432

# SMTP email — use the credentials supplied by your mail provider.
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=vasudharicemill@gmail.com
EMAIL_HOST_PASSWORD=fqas tjbt fpkz mprw
DEFAULT_FROM_EMAIL=vasudharicemill@gmail.com

